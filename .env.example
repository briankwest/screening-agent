# SignalWire Configuration
# Your SignalWire project credentials (if using auth)
# SIGNALWIRE_PROJECT_KEY=your_project_key
# SIGNALWIRE_TOKEN=your_token

# Server Configuration
# Note: Port 5000 is used by AirPlay on macOS, so we use 5001
PORT=5001
HOST=0.0.0.0

# Public URL Configuration (optional)
# If behind a proxy/tunnel (ngrok, cloudflare, etc.), set your public URL here
# The SDK will use this for all webhook URLs (hold music, dest_swml, etc.)
# If not set, URLs are auto-detected from X-Forwarded headers or request host
# SWML_PROXY_URL_BASE=https://briankwest.ngrok.io

# Phone Number Configuration
# TO_NUMBER: The human agent's phone number to dial when a call needs screening
TO_NUMBER=+12623098501

# FROM_NUMBER: The caller ID to use for outbound calls and connect actions
FROM_NUMBER=+12073067597

# SWML Authentication
# Credentials for the dest_swml URL when dialing out to CallAgent
# The URL will be formatted as: https://user:password@host/call-agent
SWML_BASIC_AUTH_USER=signalwire
SWML_BASIC_AUTH_PASSWORD=your_password_here
